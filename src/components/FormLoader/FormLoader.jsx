import Loader from "components/ReusableComponents/Loader/Loader";
import { Container } from "./FormLoader.styled";

const FormLoader = () => {
    return (
        <Container>
            <Loader/>
        </Container>
    )
}

export default FormLoader;